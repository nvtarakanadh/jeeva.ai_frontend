# Installing i18n Dependencies

To enable language functionality, you need to install the following packages:

```bash
cd Jeeva_AI_FrontEnd
npm install i18next react-i18next
```

If you encounter any npm errors, try:
```bash
npm cache clean --force
npm install i18next react-i18next
```

After installation, the language functionality will work automatically!
